{
  "info": {
    "_postman_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "name": "Petstore Users API - QA Challenge",
    "description": "Colección completa de pruebas automatizadas para el CRUD de usuarios en PetStore Swagger API.\nFlujo: Crear → Consultar → Actualizar → Verificar actualización → Eliminar.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "base_url",
      "value": "https://petstore.swagger.io/v2",
      "type": "string"
    },
    {
      "key": "username",
      "value": "kelinqa",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "1. Create User",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "// Pre-request: log del request que se va a enviar",
              "console.log('Creando usuario:', pm.variables.get('username'));"
            ],
            "type": "text/javascript"
          }
        },
        {
          "listen": "test",
          "script": {
            "exec": [
              "// ── Validar status code 200 ──────────────────────────────",
              "pm.test('Status code es 200', function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "// ── Validar que el Content-Type es JSON ─────────────────",
              "pm.test('Content-Type es application/json', function () {",
              "    pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');",
              "});",
              "",
              "// ── Validar estructura del response body ────────────────",
              "pm.test('Response body tiene estructura correcta', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body).to.have.property('code');",
              "    pm.expect(body).to.have.property('type');",
              "    pm.expect(body).to.have.property('message');",
              "});",
              "",
              "// ── Validar que el type es 'unknown' (respuesta estándar) ─",
              "pm.test('Response type es unknown', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.type).to.eql('unknown');",
              "});",
              "",
              "// ── Validar tiempo de respuesta aceptable ───────────────",
              "pm.test('Tiempo de respuesta es menor a 5000ms', function () {",
              "    pm.expect(pm.response.responseTime).to.be.below(5000);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"id\": 1001,\n  \"username\": \"{{username}}\",\n  \"firstName\": \"Kelin\",\n  \"lastName\": \"Ferrera\",\n  \"email\": \"kelin@test.com\",\n  \"password\": \"12345\",\n  \"phone\": \"99999999\",\n  \"userStatus\": 1\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url}}/user",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "user"
          ]
        },
        "description": "Crear un nuevo usuario en la API de PetStore con los datos de Kelin Ferrera"
      },
      "response": []
    },
    {
      "name": "2. Get User",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "// ── Validar status code 200 ──────────────────────────────",
              "pm.test('Status code es 200', function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "// ── Validar que el username es correcto ──────────────────",
              "pm.test('Username es el esperado', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.username).to.eql(pm.variables.get('username'));",
              "});",
              "",
              "// ── Validar estructura del response ──────────────────────",
              "pm.test('Response tiene todos los campos esperados', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body).to.have.property('id');",
              "    pm.expect(body).to.have.property('username');",
              "    pm.expect(body).to.have.property('firstName');",
              "    pm.expect(body).to.have.property('lastName');",
              "    pm.expect(body).to.have.property('email');",
              "});",
              "",
              "// ── Validar que firstName es el original ──────────────────",
              "pm.test('firstName es Kelin (valor original)', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.firstName).to.eql('Kelin');",
              "});",
              "",
              "// ── Validar email original ────────────────────────────────",
              "pm.test('email es kelin@test.com (valor original)', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.email).to.eql('kelin@test.com');",
              "});",
              "",
              "// ── Validar tiempo de respuesta ───────────────────────────",
              "pm.test('Tiempo de respuesta es menor a 5000ms', function () {",
              "    pm.expect(pm.response.responseTime).to.be.below(5000);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{base_url}}/user/{{username}}",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "user",
            "{{username}}"
          ]
        },
        "description": "Obtener el usuario creado por su username para verificar que fue creado correctamente"
      },
      "response": []
    },
    {
      "name": "3. Update User",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "// ── Validar status code 200 ──────────────────────────────",
              "pm.test('Status code es 200', function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "// ── Validar estructura del response ──────────────────────",
              "pm.test('Response body tiene estructura correcta', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body).to.have.property('code');",
              "    pm.expect(body).to.have.property('type');",
              "    pm.expect(body).to.have.property('message');",
              "});",
              "",
              "// ── Validar código de respuesta exitoso ──────────────────",
              "pm.test('Código de actualización es 200', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.code).to.eql(200);",
              "});",
              "",
              "// ── Validar tiempo de respuesta ───────────────────────────",
              "pm.test('Tiempo de respuesta es menor a 5000ms', function () {",
              "    pm.expect(pm.response.responseTime).to.be.below(5000);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "PUT",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"id\": 1001,\n  \"username\": \"{{username}}\",\n  \"firstName\": \"Kelin Actualizado\",\n  \"lastName\": \"Ferrera\",\n  \"email\": \"nuevo@test.com\",\n  \"password\": \"12345\",\n  \"phone\": \"88888888\",\n  \"userStatus\": 1\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url}}/user/{{username}}",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "user",
            "{{username}}"
          ]
        },
        "description": "Actualizar el firstName y email del usuario existente"
      },
      "response": []
    },
    {
      "name": "4. Get Updated User",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "// ── Validar status code 200 ──────────────────────────────",
              "pm.test('Status code es 200', function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "// ── Validar que el firstName fue actualizado ─────────────",
              "pm.test('firstName fue actualizado correctamente', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.firstName).to.eql('Kelin Actualizado');",
              "});",
              "",
              "// ── Validar que el email fue actualizado ─────────────────",
              "pm.test('email fue actualizado correctamente', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.email).to.eql('nuevo@test.com');",
              "});",
              "",
              "// ── Validar que el phone fue actualizado ─────────────────",
              "pm.test('phone fue actualizado correctamente', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.phone).to.eql('88888888');",
              "});",
              "",
              "// ── Validar que username no cambió ────────────────────────",
              "pm.test('username permanece igual', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.username).to.eql(pm.variables.get('username'));",
              "});",
              "",
              "// ── Validar tiempo de respuesta ───────────────────────────",
              "pm.test('Tiempo de respuesta es menor a 5000ms', function () {",
              "    pm.expect(pm.response.responseTime).to.be.below(5000);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{base_url}}/user/{{username}}",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "user",
            "{{username}}"
          ]
        },
        "description": "Verificar que los datos del usuario fueron actualizados correctamente"
      },
      "response": []
    },
    {
      "name": "5. Delete User",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "// ── Validar status code 200 ──────────────────────────────",
              "pm.test('Status code es 200', function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "// ── Validar estructura del response ──────────────────────",
              "pm.test('Response body tiene estructura correcta', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body).to.have.property('code');",
              "    pm.expect(body).to.have.property('type');",
              "    pm.expect(body).to.have.property('message');",
              "});",
              "",
              "// ── Validar que el mensaje incluye el username eliminado ──",
              "pm.test('El mensaje de eliminación incluye el username', function () {",
              "    const body = pm.response.json();",
              "    pm.expect(body.message).to.include(pm.variables.get('username'));",
              "});",
              "",
              "// ── Validar tiempo de respuesta ───────────────────────────",
              "pm.test('Tiempo de respuesta es menor a 5000ms', function () {",
              "    pm.expect(pm.response.responseTime).to.be.below(5000);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "DELETE",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{base_url}}/user/{{username}}",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "user",
            "{{username}}"
          ]
        },
        "description": "Eliminar el usuario creado durante las pruebas para limpiar el estado"
      },
      "response": []
    }
  ]
}
